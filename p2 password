#if password matches, print welcome user
class password_check:
    def check(self,user_password,current_password):
        if(user_password==current_password):
            print("welcome")
        else:
            print("denied access")
user_password=input("plz enter the password")
obj =password_check()
obj.check("saloni",user_password)
